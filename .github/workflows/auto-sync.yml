name: Auto Sync LightCast

on:
  push:
    branches:
      - main
  schedule:
    # רץ כל יום בשעה 4:00 בבוקר (שעון UTC = 6:00 בבוקר שעון ישראל בחורף)
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Setup Git
        run: |
          git config --global user.name "AnLoMinus"
          git config --global user.email "actions@github.com"
      
      - name: Sync changes
        run: |
          echo "🔄 סינכרון אוטומטי של LightCast Electric Geula"
          echo "⚡ Powered by Divine Sparks"
          
          # כאן ניתן להוסיף לוגיקת סנכרון נוספת
          # לדוגמה: עדכון תאריכים, יצירת קבצים חדשים, וכו'
          
      - name: Commit changes (if any)
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            git add .
            git commit -m "🔄 Auto-sync: עדכון אוטומטי $(date '+%Y-%m-%d %H:%M:%S')"
            git push
          else
            echo "✅ אין שינויים לסנכרון"
          fi

